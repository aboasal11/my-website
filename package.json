{
  "name": "portfolio-contact-server",
  "version": "1.0.0",
  "description": "Small Express server to forward contact form submissions via SMTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
